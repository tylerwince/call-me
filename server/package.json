{
  "name": "textme-mcp",
  "version": "1.0.0",
  "description": "MCP server that lets Claude text you via iMessage, Telegram, or Slack",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "prestart": "bun install --silent",
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@slack/bolt": "^4.1.0",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "bun-types": "latest"
  }
}
